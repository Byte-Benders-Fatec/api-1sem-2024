Following is the database schema.

DROP TABLE IF EXISTS cliente;
create table cliente (
	cli_id int primary key auto_increment,
    cli_nome varchar(30)
);

DROP TABLE IF EXISTS alunos;
create table alunos(
	aluno_id int primary key auto_increment,
    aluno_nome varchar(30)
    );

Generate only a valid (syntactically correct) executable Postgres SQL query (without any explanation of the query) for the following question (translate from portuguese to english unless if you find table name):
`<question>`:
